# 精选选股流程代码审查与修复

2026-09-12更新：本报告是第五批审查基线，部分问题已在第六批修复。当前进展以《精选选股第六批整改记录》为准；R01–R04仍有未关闭验收项，T10发布门槛不变。

日期：2026-09-11。范围仅智诊盯盘。审查以前三批实际代码与T10测试为依据；本轮单代理执行，不代表外部独立策略验证或收益有效性证明。

## 本轮已修复

| 范围 | 修复后的行为 | 验证 |
| --- | --- | --- |
| T04/T06 K线 | 真实日期、正数OHLC及高低价关系校验；重复日期不增加深度；尾日期必须等于目标日期 | kline-quality、candidate-review |
| T06 原策略 | 同次规则快照用于检测；命中必须同时对应规则ID和形态ID，不接受另一条同形态规则代替 | candidate-review、watch-recommendation |
| T07 失效与保存 | 当前K线日期缺失即失效；复核版本升至candidate-review-v3；候选保存失败返回失败 | recommendation-validity及代码审查 |
| T08 迁移 | 明确空codes不迁移；非法类型报错；写入前重新核对候选，删除/更换批次则跳过；单只异常记录后继续；目标已保存而原池删除失败保留重试信息 | pool-watch-migration：空输入、删除/替换竞态、失败与幂等 |
| T09 操作 | 评估按钮明确传false，只有布尔true才重试；store暴露清空/移除函数；默认迁移只发送明确有效的精选代码，缺validity不授权迁移 | pool-actions执行实际store函数，网络和状态隔离 |
| T10 回放 | 缺历史字段/日期错误不能通过；坏未来bar不生成虚假收益；按日期/批次及历史参数分别配额；重复/参数冲突批次不记已验证 | selection-replay及隔离链路测试 |
| T10 只读 | 捕获候选JSON与数据库字节，内存查询，不初始化/导出生产数据库；不套用当前规则补历史；输出输入哈希、逐样本观察和持续阻断原因 | 本地114条只读回放 |

## 尚未关闭的问题与下一批顺序

| 编号 | 优先级/任务 | 实际缺口 | 验收条件 |
| --- | --- | --- | --- |
| R01 | P0 / T02–03 | 预扫描主要按自然日失效；板块asOf使用请求日期；逐股证据共用最晚行情源时间，同日时龄与旧资金流拼接未闭环 | 逐源逐股真实时间、TTL和交易日历统一；过期/缺时间降级且可恢复 |
| R02 | P0 / T04、T07 | 多源K线优先长度而非新鲜度；复权可能不一致、UPSERT可能混合历史；推荐仅对比日期，同日内容更新不会失效 | 版本/内容指纹与复权一致性校验；同日修改立即失效；交易日缺口验收 |
| R03 | P0 / T06–08 | 后台复核保存异常缺完整finally兜底，可能停留running；规则未整批冻结，盘中尾bar与收盘确认语义不足 | 保存失败/取消均释放任务；整批版本固定；跨日及收盘边界隔离回归 |
| R04 | P0 / T05–06 | 初筛null涨幅可能变0，limit=0回退默认；配额函数自身无代码去重；复核未全面重验初筛硬门槛与题材证据 | 缺值拒绝、零配额、重复代码、外部构造候选均不能绕过精选门槛 |
| R05 | P0 / T10 | 缺完整市场/成分/候选全集与历史K线版本、复权、收盘状态归档；回放默认isFinal，不能证明完整无时点偏差 | 先建设不可变批次归档，再收集多交易日样本，预先定观察窗口与参数评估准则 |
| R06 | P1 / T09、T10 | 多视口、键盘、缩放、加载/空/失败/恢复、减少动画/打印实操未完成 | 固定3110浏览验收并保留证据；当前浏览连接曾被运行环境阻断，不以静态断言替代 |

上述是已定位缺口，尚未逐项修复或完成真实数据验证。R01–R04先于参数定稿；R05可在基础证据契约稳定后实施。GPT-6 medium可继续承担明确边界修复；涉及复权、并发与时点归档设计可提高推理强度，这是工程建议而非官方性能结论。

## 验证结果和边界

- `npm test` 全部通过；新加入默认测试链的 `pool-actions.test.js` 及新增批次冲突断言另行执行通过。
- `npm run check`、`npm run build:frontend`、`git diff --check`通过。构建仍提示单包大于500KB；未做包体重构。
- 回放v2：114条候选、完整字段样本0、可验证配额批次0；潜力86、当天过热28、五日过热17、MA20偏离18、严格通过0。配额保留数为null，撤回旧版合并多日算20只的结论。
- 模块链路使用隔离适配器，包含mock检测器/价位；不等于真实HTTP、行情源和浏览器全链路验证。字段完整也不等于来源可信或历史全集。
- 未重启3110活动服务、未调用真实名单写接口。后端修复尚未加载到既有进程；前端已构建，需与新版后端一起做运行验收。
- 本轮前后SHA-256一致：候选池`587C637B3F033550CF3328641928FCA1A62E605491EC7748DB7CA24AE074EDC3`；K线库`FE63673B659C2DB1947E21A7D33F6EC400F1F3E45778499916262B2323318EE0`；自选`2CF1EAD61DFB445CE5F11CEA267AB188DE9E60B0658A46CEB117EF4542B7986B`。
- 先前自选19→11原因仍未知；本轮未恢复覆盖，亦未把它归因用户操作。

结论：本轮所列修复已完成自动化回归；T01–T09仍待验收，T10仍发布受阻。下一阶段按R01→R02→R03→R04推进，完整归档与浏览终验随后开展。
