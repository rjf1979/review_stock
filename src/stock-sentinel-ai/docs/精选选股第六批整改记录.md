# 第六批：时效、推荐失效、任务恢复和筛选边界

日期：2026-09-12。范围仅智诊盯盘后端及隔离测试；状态：本批代码整改已完成，运行验收未完成。

本阶段追加接入 2026 年维护式交易日表，并让开盘倒计时、闭市判断和预扫描复用共用交易日判定；未来年度仍需更新表并以官方交易日历校验。

## 实际行为

| 方向 | 本批完成 | 兼容影响 |
| --- | --- | --- |
| 行情时效 | 非收盘确认的预扫描30分钟过期；缺时间/未来时间拒绝；个股及板块源时间5分钟校验，午休按11:30、盘后按15:00计算时龄；跨日拒绝 | 过期结果仍可展示，扫股需重新扫描市场；旧预扫描缺时间不可继续使用 |
| 真实源字段 | 板块请求f124，缺响应时间不再拿请求日期冒充asOf；每股保存自己的源时间；资金流和流通市值读取同次个股行情，不拼旧快照 | 来源缺必要字段时排除，可能减少结果；不以0或旧值补齐 |
| 推荐失效 | 全部已存K线的日期/OHLC/量/额生成SHA-256；读取推荐及迁移均检查；迁移提交前再次检查；推荐行情过期亦失效 | v4推荐新增klineHash/quoteSourceAt；旧版推荐需主动重新复核，不自动调用AI或迁移 |
| 异常恢复 | 启动/进度/结果/结束保存的抛错或失败返回均收尾；释放active；整批规则及候选深拷贝；失败清理待排队项；取消不产生新精选 | 失败后可重新启动；中途失败可能已有部分落盘结果，批次状态未completed时不允许迁移 |
| 筛选边界 | null/空涨幅不能作为0%；limit=0不回退默认；配额按代码去重；严格复核再次校验初筛防追高、流动性和同日题材 | 原策略命中或高排名不能越过这些门槛；跨日候选不会因时间过去自动变成收盘确认 |

时龄为初始工程配置：预扫描30分钟、行情5分钟，未来时间容差1分钟。尚未历史验证，不作为最优参数。复核版本升为`candidate-review-v4`，初筛数值参数未调整。

## 验证

- 全量`npm test`通过；后续增加提交前K线变化和推荐过期检查后，迁移、模块链路、推荐、有效性及批任务定向回归通过。
- `npm run check`与`git diff --check`通过；本批未改前端，不重复构建或声称完成浏览验收。
- 新增`recommendation-batch.test.js`覆盖启动/进度/结果保存失败、取消、排队清理、重新启动及规则快照固定。
- 新增`kline-fingerprint-storage.test.js`使用临时SQLite，验证同日覆盖后指纹改变且读取路径一致。
- 新增`board-source-time.test.js`使用模拟接口，验证缺源时间及响应日期与请求日期不一致时不伪造时间。
- 扩充时龄、午休/收盘、空值、重复、零配额、过热、缺题材、未完成批次、迁移期间K线变化回归。
- 使用项目a-stock-data规范：严格区分源时间与请求时间；缺证据降级。本轮未进行在线取数写入或重启3110服务，运行中的后端尚未加载本批修复。

## 尚未完成，不能合并标为已验收

1. R01余项：法定交易日历仅维护当前年度；真实板块f124及个股f62/f21返回覆盖需在线只读验证，缺字段会减少甚至清空可选范围。全市场温度来源的时点一致性仍需继续核验。
2. R02余项：内容指纹能识别变化，不能证明多来源复权一致；当前按主源达到目标深度即停止，备用源需显式指定；历史混合写入和交易日缺口仍需处理。
3. R03余项：K线目前没有完整的来源收盘确认元数据；盘后判断仍结合系统时段，不能等同于已经验证每根bar最终版。任务异常信息保留在当前进程状态，重启恢复提示和任务崩溃恢复尚需完善。
4. R04余项：本批按当前默认初筛门槛复核，未建设防篡改历史证据仓库；传入字段齐全不等于已经验证其来源真实性。重复记录的跨分类冲突及全链路漏斗审计应补充验收。
5. R05–06继续阻断：不可变扫描全集/K线版本归档、多交易日参数验证、浏览实操、运行重启和发布/回退验收。

下一批优先关闭数据基础余项（交易日历、复权与来源覆盖），再建设历史归档并安排3110运行验收。可以继续GPT-6 medium；复杂复权与恢复设计建议提高推理强度，此为工程判断。

第八阶段已新增候选批次不可变归档；完整说明见《精选选股流程需求与任务安排-v2》交付记录。该归档不包含完整市场全集，历史验证门槛保持阻断。
