# 2026-08-26 会话记录

## 记忆约定（用户明确）
- 项目记忆统一存 `.codex/memory/`（`MEMORY.md` 长期索引 + `YYYY-MM-DD.md` 当日记录），不再写 `.claude` auto-memory。旧索引已加指路牌。

## 今日 git 开发记录（8 个提交，连续发布 3 个小版本，全部在 main）
- `2cec3ac` **v0.2.1 收盘行情缓存修复**：闭市后首次打开仍显示前一交易日实时行情、自选股报价缓存跨日复用旧数据；完善盘前/午休/闭市/周末的交易日边界判断（`server.js` +34、`server.test.js` +23）。
- `80b951f` **v0.2.2 桌面端在线升级**：新增 `updater.js`、`scripts/generate-update-manifest.js`、`updater.test.js`、`docs/desktop-update.md`；`electron-main.js` +86 行；启动 15s 后自动检查、每 6h 定时复查；按架构下载 x64/ia32，校验大小+SHA-256 后静默覆盖安装；设置页手动检查/重试/下载进度；官网 `updates/latest.json` + server 调整。
- `2cd849f` **v0.2.3**：免费版自选股上限 9 只（存储层强制校验）；修复闭市后日 K 停留在上一交易日；K 线源落后时用最新报价补齐当日 K 棒并标注；许可换成 `Hangqing Personal Non-Commercial Source License 1.0`（`LICENSE` 大幅改写）。
- `6d4b4f1` fix(web) 官网桌面版下载链接。
- `f244d5a` security(deploy) **生产 SSH 迁移到密钥**：删除明文"服务器配置.txt"，`.gitignore` 忽略，web client 微调。
- 工作树干净（除本记录文件）。v0.2.3 SHA-256（x64/ia32）见 `.codex/memory/MEMORY.md` 发布状态。

## 桌面版源码回顾（`src/desktop/`，当前 v0.2.3）
- 主进程 `electron-main.js`：单实例锁、无边框窗口（关闭=隐藏到托盘）、SQLite 存储（`storage.js`）、定时复盘（`review-scheduler.js`）、自动更新、IPC 桥。
- `AGENTS.md` 尚无记忆章节，记忆位置约定以 `.codex/memory/` 为准。
