# 2026-08-21 会话

## 完成
- 上传接口行情日期改为必填：`server/index.js` `applyReport`（缺 `date` 返回 400）、`scripts/upload-report.js`（日期参数必填）、`README.md` 文档、`App.vue` 上传密钥页调用说明。
- 新增后台管理 API 对接手册 `docs/api-integration-manual.md`（22 个接口、鉴权、数据模型、对接流程），`README.md` 顶部加入口。
- 端到端验证（本地 PG 免密连接）：缺 date→400、格式错→400、未来日期→400、密钥错→401、正常上传→201（UTF-8 无损、HTML/MD 落盘、交易日标记 analyzed）。测试假数据（2026-08-17 报告 + PG 交易日记录）已清理。
- 项目记忆落地到 `.codex/memory/`（本文件为会话日志，持久约定见 `MEMORY.md`）。
- 后台新增「API 对接手册」菜单（`/admin/api-manual`）：新增 `GET /api/admin/api-manual`（后台鉴权，读 `docs/api-integration-manual.md` 并把 `{BASE}`/`{UPLOAD_KEY}` 占位符替换为真实值），页面展示真实接入信息（Base URL / 上传密钥，可显示隐藏与逐项复制）+ 全文 `<pre>` + 复制按钮。
- 手册收窄为**仅上传每日行情 MD 接口**（`POST /api/upload/report`）；「上传密钥」菜单/页面、`PUT /api/admin/settings/upload-key` 接口已删除，密钥改由手册页展示复制；密钥更换需直接改 PG `app_settings.upload_key`。

## 注意
- 后台 AI 设置模型为 `gpt-5.6-luna`：若服务商不识别，AI 排版会静默回退默认渲染；codex 记忆任务的报错也正是该模型名不存在（API 支持 `deepseek-v4-pro` / `deepseek-v4-flash`）。
