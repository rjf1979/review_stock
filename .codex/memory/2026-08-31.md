# 2026-08-31 · Android 真机联调

## 当前结果
- 早期 uni-app 已移除，`src/app_flutter/` 是唯一移动端工程。APK 图标/名称已对齐桌面版「股市脉搏」。
- 已修复线上行情契约：指数价格与成交额兼容字段、盘口异动嵌套结构、东财采集参数与 secid 处理。
- 自选股缺数时通过 App 众包补 quotes；龙虎榜当日未披露时自动回退最近交易日。
- 设置页移除云端 API/状态，改为「外观 / 实时刷新 / 关于」分区；刷新频率为横向滚动胶囊选项，5–60 秒、5 秒步进，持久化到本机安全存储。
- 午间快照保护已在 App、PC 和 mapi 三层落地：只在交易窗口补采，低完整度同日快照不覆盖完整快照。

## 验收
- `flutter analyze` 0 问题；`flutter test` 9/9 通过；mapi 22/22 通过。
- 最新 debug APK 180,317,054 字节，SHA-256 `B2223896A4734ED9287FF15E744E4C74D17BFEC1F084D263CE99101CC8F4F39E`。
- 真机选择 15 秒后，实时页显示「前台每 15 秒自动刷新」；强制重启后设置仍为 15 秒。
- mapi release `20260831132759`，服务 active，公网 status 2xx。

## 风险与后续
- 当前仅 debug APK，后续发布前应走签名 release 验证。
- 华为真机安装可能仍要求手动确认风险/ICP 提示。


## 2026-08-31 下午 · 桌面 v0.3.9 发布（实时盘口优化）
- 针对后台 PC 错误日志「[实时] 猛烈打压失败: The operation was aborted due to timeout」，优化 `src/desktop/review-core.js`（可配置超时、盘口并发上限 3、每类 8s 超时+600ms 重试、最近数据兜底 `stale`、缓存 TTL 8s）与 `server.js`（新增 `loadPankou()` 独立缓存 + 6s Promise.race，盘口超时不拖累主实时聚合）。
- 升版 0.3.9：`package.json` / `package-lock.json` / `installer.nsi` / `frontend/changelog.json` 已同步。
- 本地回归全绿：`npm run check`、`npm test` 通过；`npm run dist` + NSIS 双架构产物：
  - `release/hangqing-desktop-0.3.9-win-x64-setup.exe` 85,209,065 B，SHA256 `eac51a9f...129334`
  - `release/hangqing-desktop-0.3.9-win-ia32-setup.exe` 80,489,810 B，SHA256 `a62cb10f...3e674ee`
- 升级清单 `src/web/public/updates/latest.json` 已写 v0.3.9，OSS `deploy_oss.py` 上传成功，`https://oss.askcode.cn/files/...` 回读 200 且字节/MD5 一致。
- GitHub Release `v0.3.9`（repo `rjf1979/review_stock`）已正式发布：`gh release view` 确认 isDraft=false、两资产 digest 与本地 SHA 完全一致；本机代理无法直连 GitHub 资产下载（connection reset），但上传本身成功。
- 卡点：官网 `https://dailystock.zhicha.io/api/status` 返回 404，`/updates/latest.json` 公网回读仍是 v0.3.8（服务器文件已是 0.3.9），疑似 Nginx/CDN 缓存或线上服务未重启；Git 提交与推送（HTTPS+gh token，SSH 公钥不可用）尚未完成。
