# 2026-08-22

- 桌面端完成资金流、要闻、自选股复盘展示和异步状态处理。
- Electron 增加托盘常驻、窗口隐藏/恢复、通知开关和工作日 15:35 后自动复盘。
- 自动复盘 Markdown 保存在 Electron 用户目录 `reviews/`，同日去重，失败下次 tick 重试。
- 本地仓库已推送到 GitHub `main`，后续继续开发需保留桌面端 Electron 原型路径。
- 桌面端刷新改为视图级更新：实时行情/大盘概况只请求行情接口，定时刷新仅在这两个视图运行；顶部统一显示 loading tip，刷新期间保留已有数据。
- 2026-08-22 交互改动后已重启 Electron，窗口标题为「行情日报 Desktop」，本地服务 `http://localhost:3100` 返回 200。
- 实时行情改为静默局部更新：不显示 loading/遮盖层，不改变刷新按钮状态，只更新 Vue 数据节点；其它视图保留内容区遮盖式 loading。
- Electron 主进程加入 `requestSingleInstanceLock`，避免重复启动导致 `3100` 端口占用弹框。
- 实时行情按 A 股交易时段控制请求：工作日 `09:30-11:30、13:00-15:00` 静默更新，其余时间停止行情请求并提示闭市原因与下次开市时间；状态每 30 秒检查一次。
- 闭市启动或进入实时行情时允许单次请求获取最后交易快照，页面保留数据显示并标记「最近获取」，之后不再持续轮询。
- 今日开发结束：桌面端当前已运行，窗口标题为「行情日报 Desktop」，本地服务 `http://localhost:3100` 返回 200；工作区修改尚未提交，下一次从桌面端交互验收、提交和推送继续。
