# 2026-08-30 会话记录

## 官网新增「实时行情」板块（已完成并上线）
- 需求：桌面版 v0.3.8 已默认上传云端，用户希望在 web 平台看到同步更新的行情。决策：页面放 **zhicha 官网服务器**（dailystock.zhicha.io），不动内存紧张的 askcode 行情 API 服务器。
- `src/web/server/index.js`：新增 GET `/api/*` 只读反代到 `https://api.dailystock.askcode.cn`（`MARKET_API_ORIGIN` 可覆盖），解决跨域；按协议选 http/https 模块，15s 超时。
- `src/web/client/src/App.vue`：新增 `#live` 板块 + 导航入口。同步状态条（fresh/stale/等待上传徽标 + 数据时间北京时间 + 桌面版版本 + 行情日期 + 立即刷新）；指数卡（红涨绿跌）、市场宽度（涨跌家数/红盘率/涨跌停/成交）、涨停梯队前10、板块主力资金净流入/流出；60s 轮询（`document.hidden` 时跳过），空态/过期态明确提示。`theme.css` 加 `.live-*` 样式；`vite.config.js` 加 dev 代理。
- 本地验证：`src/web/scripts/` 新增 `test-live-proxy.mjs`（反代冒烟）、`mock-market-api.mjs`+`mock-stack.mjs`（mock 行情 API 起全栈，mock 数据含指数/宽度/涨停/资金流）、`screenshot-live-mock.mjs`（playwright-core 驱动系统 Edge 截图，require 路径指向 src/desktop/node_modules）。空态和 mock 全量渲染都截图验证过。
- 部署：`.deploy/deploy-web-live.sh`（检查布局→备份 `.bak-live`→scp server/index.js+app.mjs+app.css 到 `/var/www/dailystock`→`systemctl restart dailystock`→curl 验证）。官网部署布局：`/var/www/dailystock` + systemd `dailystock.service`（内部 3002），nginx 配置在 `.deploy/dailystock.nginx.conf`。
- 线上验证通过：`https://dailystock.zhicha.io/#live` 渲染正常，`/api/status`、`/api/realtime` 反代返回线上数据（当前为周日：只有心跳 v0.3.8，realtime head 是今早 mapi 端到端测试的 probe 残留，周一开盘后会被真实数据覆盖）。
- 注意：AGENTS.md 仍写着「官网不承载行情采集/报告上传」，本轮已按用户要求突破该边界，文档尚未同步更新。


## 提交与推送
- `161041c`：Flutter 移动端 + mapi 云端众包后端骨架 + docs + 记忆；补 mapi package-lock，把 Flutter pubspec.lock 从 .gitignore 移出后入库。

## 官网发布与监听收敛（2026-08-30 完成）
- 已按仓库源码发布纯官网：`server/index.js`、`assets/app.mjs`、`assets/app.css`、`updates/latest.json` 上线 zhicha `/var/www/dailystock`。
- 远端回滚点：`/var/www/dailystock/.backup/202608301850/`；Node 已改为仅监听 `127.0.0.1:3002`，`systemd dailystock` active。
- 线上验证：`/`、`/assets/app.mjs`、`/assets/app.css`、`/updates/latest.json` 均 200；清单版本 `0.3.8`。

## 早期 uni-app 移除（2026-08-30 完成）
- 用户确认废除早期 uni-app；已删除 `src/app/` 源码、依赖和构建缓存。
- 文档已统一指向 `src/app_flutter/`；`docs/mobile-architecture.md` 保留接口契约并标注 uni-app 方案废弃。

## Android 构建收尾（2026-08-30 完成）
- 已安装 JDK 17、Android SDK/Platform 36、Build Tools、CMake、AEHD 与 API 36 x86_64 镜像，SDK 路径 `D:\AndroidSdk`；创建 Pixel 7 API 36 AVD。
- Gradle 分发包切腾讯镜像，Google/Maven 依赖切阿里云镜像；真机华为 Mate 9 已被 adb 识别。
- `flutter build apk --debug` 在英文路径链接下成功，产物 `D:\Projects\hangqing_app_ascii\build\app\outputs\flutter-apk\app-debug.apk`，大小 158,471,640 字节，SHA-256 `169d7a74ece9786680e9fc9248c6bfecab4813c8ec786e904a7b32a79d8ce383`；按要求未安装真机。

## 官网性能优化（2026-08-30 完成）
- 慢因：4 张 PNG 截图被 Vite lib 模式内联进 `app.mjs`，导致 JS 约 848KB；`.mjs` 又不是 Cloudflare 默认缓存扩展。
- 优化：截图转 WebP 并用 `?no-inline` 拆分，入口改名 `app.js`，hashed WebP 使用 immutable 缓存，HTML 增加边缘短缓存头。
- 结果：主包约 113KB（gzip 约 36KB），静态资源 Cloudflare `HIT`；首页 HTML 仍为 Cloudflare `DYNAMIC`，如需 HTML HIT 需在 Cloudflare 配置 Cache Rule。

## WebP 图片修复（2026-08-30 完成）
- 问题：`?no-inline` 拆分后 Vite 输出 `/xxx.webp?no-inline`，服务器按 `/assets/*` 解析导致 404。
- 修复：`vite.config.js` 设置 `base: '/assets/'`；首页脚本引用升级为 `/assets/app.js?v=2`。
- 验证：无头浏览器四张图片 `naturalWidth=1424`、`naturalHeight=835`，当前页面无 4xx 资源请求。
- `363060c`：PC 云端共享默认开启并移除用户开关。
- `2498b43`：mapi OSS 修复（put 传 Buffer + 显式 endpoint）。本轮新增，已推送 origin/main。
- 均已推送 origin/main；本地剩 `monitor-screenshot.png`（临时截图，未入库）。

## PC 默认分享 + 去开关（完成并验证）
- `storage.js`：`cloud_enabled:true`、`cloud_url=DEFAULT_CLOUD_URL=https://api.dailystock.askcode.cn`，导出 `DEFAULT_CLOUD_URL`。
- `server.js`：`createCloudPush.configure` 的 `enabled` 恒真、`url` 回退内置默认；`startServer` 启动心跳改为默认分享+设备鉴权，失败仅记录不阻塞。
- 前端设置页（`frontend/index.html`）：移除「手机云同步」复选框、云端接口地址、上传令牌三项控件，改为说明性文字。
- `cloud-upload.js`：无 token 时懒注册设备 Bearer（保留 X-Upload-Token 兼容路径），无需改动。
- 新增 `cloud-upload.test.js`（首次注册 + 复用不重复注册），`storage.test.js` 加默认分享断言；`npm test` / `npm run check` 全部通过。

## mapi 云端部署（2026-08-30 上线）
- 目标 `api.dailystock.askcode.cn`（47.92.170.168，Debian 13，SSH 别名 `api-dailystock`）。自装 PG17，`pg_hba` 全 trust + 只回环，建 `hangqing_mapi` 库；部署 `/opt/apps/hangqing-mapi/{current,shared}`；systemd `hangqing-mapi`；nginx 单站点 + certbot SSL；Node 只监听 `127.0.0.1:3102`。
- OSS：bucket `my-soft-2026`（cn-shanghai），CDN `oss.askcode.cn`，前缀 `hangqing/`。
- `src/mapi/oss.js` 两处 bug 已修并上传（`2498b43`）：`putPublic` 需 `Buffer.from(...)`；客户端需显式 `endpoint=oss-cn-shanghai.aliyuncs.com`。
- 端到端验证 1–7 全部通过（status/device 鉴权/去重 stale/认领 won/OSS 直链/HTTP→HTTPS）。验证后测试数据已清（PG 三表 0/0/0，OSS 对象已删），空态复验通过。

## 服务器清理（用户追加，已完成）
- Nginx：该机原未装 Nginx；本次装后 `sites-enabled` **只保留 `api.dailystock`**，删默认站点。其余非 Web 服务（8111 python、8765 等）未动。
- chitu：停用并清理 `chitu-market`(8765 uvicorn) + `chitu-web`(8011 Nuxt) 两个单元，删单元文件 + daemon-reload；`/opt/chitu`（818MB）移到 `/root/chitu-removed-20260830113742/`（可恢复备份）；chitu 用户已删，8011/8765 已释放。
- chitu 数据库：**用的不是 PG，而是 SQLite**（`shared/runtime/database/chitu.db`，489MB），PG 里无 chitu 库；已直接删除该 SQLite（含 -wal/-shm）。PG 现仅 `hangqing_mapi`/`postgres`/template。

## 未决风险
- 服务器可用内存偏小（约 762M），PG + mapi 已占一部分，需留意 OOM；必要时调 `shared_buffers` / 减少缓存。
