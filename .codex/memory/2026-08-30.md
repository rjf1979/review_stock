# 2026-08-30 会话记录

## 提交与推送
- `161041c`：Flutter 移动端 + mapi 云端众包后端骨架 + docs + 记忆；补 mapi package-lock，把 Flutter pubspec.lock 从 .gitignore 移出后入库。
- `363060c`：PC 云端共享默认开启并移除用户开关。
- `2498b43`：mapi OSS 修复（put 传 Buffer + 显式 endpoint）。本轮新增，已推送 origin/main。
- 均已推送 origin/main；本地剩 `monitor-screenshot.png`（临时截图，未入库）。

## PC 默认分享 + 去开关（完成并验证）
- `storage.js`：`cloud_enabled:true`、`cloud_url=DEFAULT_CLOUD_URL=https://api.dailystock.askcode.cn`，导出 `DEFAULT_CLOUD_URL`。
- `server.js`：`createCloudPush.configure` 的 `enabled` 恒真、`url` 回退内置默认；`startServer` 启动心跳改为默认分享+设备鉴权，失败仅记录不阻塞。
- 前端设置页（`frontend/index.html`）：移除「手机云同步」复选框、云端接口地址、上传令牌三项控件，改为说明性文字。
- `cloud-upload.js`：无 token 时懒注册设备 Bearer（保留 X-Upload-Token 兼容路径），无需改动。
- 新增 `cloud-upload.test.js`（首次注册 + 复用不重复注册），`storage.test.js` 加默认分享断言；`npm test` / `npm run check` 全部通过。

## mapi 云端部署（2026-08-30 上线）
- 目标 `api.dailystock.askcode.cn`（47.92.170.168，Debian 13，SSH 别名 `api-dailystock`）。自装 PG17，`pg_hba` 全 trust + 只回环，建 `hangqing_mapi` 库；部署 `/opt/apps/hangqing-mapi/{current,shared}`；systemd `hangqing-mapi`；nginx 单站点 + certbot SSL；Node 只监听 `127.0.0.1:3102`。
- OSS：bucket `my-soft-2026`（cn-shanghai），CDN `oss.askcode.cn`，前缀 `hangqing/`。
- `src/mapi/oss.js` 两处 bug 已修并上传（`2498b43`）：`putPublic` 需 `Buffer.from(...)`；客户端需显式 `endpoint=oss-cn-shanghai.aliyuncs.com`。
- 端到端验证 1–7 全部通过（status/device 鉴权/去重 stale/认领 won/OSS 直链/HTTP→HTTPS）。验证后测试数据已清（PG 三表 0/0/0，OSS 对象已删），空态复验通过。

## 服务器清理（用户追加，已完成）
- Nginx：该机原未装 Nginx；本次装后 `sites-enabled` **只保留 `api.dailystock`**，删默认站点。其余非 Web 服务（8111 python、8765 等）未动。
- chitu：停用并清理 `chitu-market`(8765 uvicorn) + `chitu-web`(8011 Nuxt) 两个单元，删单元文件 + daemon-reload；`/opt/chitu`（818MB）移到 `/root/chitu-removed-20260830113742/`（可恢复备份）；chitu 用户已删，8011/8765 已释放。
- chitu 数据库：**用的不是 PG，而是 SQLite**（`shared/runtime/database/chitu.db`，489MB），PG 里无 chitu 库；已直接删除该 SQLite（含 -wal/-shm）。PG 现仅 `hangqing_mapi`/`postgres`/template。

## 未决风险
- 服务器可用内存偏小（约 762M），PG + mapi 已占一部分，需留意 OOM；必要时调 `shared_buffers` / 减少缓存。
