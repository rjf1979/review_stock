# 2026-09-01 · 桌面版统一安装应用

## 任务
新增「股市脉搏安装助手」：自动完成环境检查、架构选择、安装包下载、SHA-256 校验，并启动 Windows 安装向导。

## 当前进展
- 已实现独立 Electron 安装助手：Windows/架构/磁盘/运行库检查、中文结果、下载进度、三次重试、校验失败提示和自动唤起安装器。
- 架构使用 Node/Electron `process.arch`，支持 x64/ia32；安装包来源复用现有升级清单。
- `src/desktop/installer-core.js` 覆盖核心逻辑；`src/desktop/installer-app/` 覆盖 UI；构建配置为 `src/desktop/installer-builder.yml`。
- 已生成 x64、ia32 和双架构合一 portable；x64/ia32 目录构建与完整打包均通过。
- `npm run check`、`npm test` 全绿；当前机器验证 x64、VC++ 运行库和已安装版本识别正常。
- 安装说明已补入 `docs/desktop-install.md`。
- 官网与 README 已改为推荐统一安装应用，官网源码和静态产物同步到 0.3.9；OSS 公开链接回读 200，大小和 SHA-256 与本地一致。
- 官网已上线 release `20260901-132042`：静态资源缓存版本 `v=3`，`latest.json` 已切换到 0.3.9，服务 active 且日志无错误。
- 安装助手已同步 OSS 与 GitHub Release v0.3.9；两个公开下载链接均可回读 200，大小和 SHA-256 一致。
- 相关代码已按模块提交并推送到 `origin/main`；SSH/HTTPS 直连失败时可用 Git Data API 方案。

## 待验收
- x64 与 ia32 环境各完成一次端到端下载、进度、SHA-256 和真实安装器启动验证。
- 验证架构不符、磁盘不足、下载中断、校验失败和 SmartScreen 提示。
- 发布时上传安装助手并记录 SHA-256，同时勾除本工作项。
