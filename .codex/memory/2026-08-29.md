# 2026-08-29 会话记录

## App 端众包数据采集方案定稿（未开发）

- 用户确定：移动端技术栈改为 **Flutter**；采集从「PC 唯一」扩展为 **多用户公开众包**，核心诉求是「谁先采集谁先共享」——用阿里云 OSS + 独立 API 实现。
- 定稿方案已落地为 `docs/app-crowdsource-collection.md`（设计文档，替代 `docs/mobile-architecture.md` 里「PC 唯一采集」的数据流描述，接口契约整体兼容）。
- 核心设计要点：
  - 去重键 = 数据逻辑身份（时间桶），不是设备时钟；`hangqing/review/<日期>/<mode>.json` 等确定性键。
  - 两类写入语义：**槽位认领**（first-writer-wins，Postgres `INSERT ON CONFLICT DO NOTHING` 原子仲裁，用于复盘/龙虎榜/日K）与 **头指针更新**（freshest-wins + 时间戳单调守卫，用于实时快照/报价）。
  - OSS AK/SK 只放 API 服务端（复用 `.deploy/aliyun-oss.env`，bucket `my-soft-2026`）；客户端只拿 Device Token，读走 public-read + CDN（`oss.askcode.cn/hangqing/`）。
  - 多用户必备：设备注册鉴权、按设备限流、schema+合理性校验、trust 分级。
- 规划范围：数据层 + Flutter 整体；分 6 阶段（Phase 0 OSS 准备 → 1 mapi 升级 → 2 PC 接入 → 3 Flutter 骨架 → 4 Flutter 采集端 → 5 加固）。
- **用户明确：先不开发**，回来再细细看。当前仅完成文档与记忆落地，未改任何代码。
- 风险已标注：众包 = 把抓自腾讯/东财的公开数据再公开分发，涉数据源 ToS/合规，方案不背书合规。

## 部署目标确认（用户补充）

- 独立行情 API 部署服务器：`api.dailystock.askcode.cn` → `47.92.170.168`（阿里云，已在 `~/.ssh/known_hosts`）；该服务器自带 PG。
- SSH 密钥候选：`~/.ssh/deploy_market_daily`（ed25519，注释 `deploy-market-daily`）；`~/.ssh/config` 尚无该服务器别名（现仅 `zhicha-vps`=107.148.27.165:49073），部署前需补 Host 别名。
- 用户：调整完部署信息后**可以开始开发**。

## 已开发完成（Phase 1 + 2）

- **Phase 1 · mapi 后端升级**（`src/mapi/`，已单测 21/21 通过 + file 模式冒烟）：
  - 新增模块 `keys.js`（对象键派生）/`validate.js`（校验）/`rateLimit.js`（限流）/`auth.js`（HMAC 签名设备 token）/`db.js`（PG 三表 + 原子认领 + 单调头指针）/`oss.js`（ali-oss 封装）/`claim.js`（仲裁编排）+ `schema.sql`（devices/slots/heads）。
  - `server.js` 重构为双模式：`file`（默认，向后兼容旧部署）+ `cloud`（`MAPI_MODE=cloud`，OSS+PG 众包）。`package.json` 加 `ali-oss`/`pg` 依赖 + `npm test`（node --test）。
  - 关键修复：并发认领落败方 `url` 由确定性 objectKey 直接派生（不依赖获胜方是否写完），加 `ready` 标记。
- **Phase 2 · PC 接入设备鉴权**（`src/desktop/`，`npm test` 4 套件通过）：
  - `cloud-upload.js` 加 `registerDevice`/`ensureDevice`/`authHeaders`（Bearer），懒注册；`storage.js` 加 `cloud_device_id`/`cloud_device_token` 持久化；`server.js` 的 `configure` 传设备信息 + `onDevice` 回写，`run` 不再强依赖旧 token。
  - mapi `/auth/device` 支持幂等注册（接受稳定 deviceId，`auth.sanitizeDeviceId`）。
- **Phase 5 加固**：`db.js` 加 `releaseStaleClaims` 清扫 claimed 超时槽位；`validate.js` 加报价负价格校验；`/api/status` 加 `realtimeAt`/`quotesAt` 头新鲜度；`server.js` 加 `startReaper()`（`MAPI_CLAIM_TTL_MS` 默认 2min、`MAPI_REAP_INTERVAL_MS` 默认 1min）。
- **Phase 3/4 · Flutter 骨架 + 采集端**（`src/app_flutter/`，纯 Dart 源码，**本机未装 Flutter/Dart SDK，未 run analyzer**，待 `flutter create .` + `flutter pub get` 后验证）：
  - `core/`：`config.dart`（`MAPI_URL` dart-define，默认 `api.dailystock.askcode.cn`）、`auth.dart`（`DeviceAuth`，secure storage 持久化 + 幂等重注册）、`api_client.dart`（`ApiClient`，读公开/写带 Bearer）、`format.dart`（红涨绿跌）。
  - `data/`：`models.dart`（Stock/IndexQuote/RealtimeSnapshot/ReviewEntry/DragonItem）、`api.dart`（`MarketApi` 读/写接口映射）、`sources.dart`（`MarketSource` 东财抓取骨架，腾讯 GBK/K线/TODO 标注）。
  - `services/`：`collector_service.dart`（`ensureRealtime`/`ensureQuotes`/`ensureReview`/`reportHeartbeat`，「先采集先共享」闭环，判断新鲜度→抓源→认领）。
  - `state/providers.dart`（riverpod providers）、`features/home_page.dart`（7 视图导航骨架）、`main.dart`（引导 + 后台采集不阻塞首屏）。
  - pubspec：`dio`/`flutter_riverpod`/`flutter_secure_storage`。所有 Dart 均标 TODO 说明（K线/腾讯GBK/复盘聚合需复刻 PC 后对齐）。
- **待办（已部分完成）**：把 mapi 部署到 `api.dailystock.askcode.cn`（PG/OOS/nginx/JWT 密钥）。

## Flutter SDK 安装（2026-08-29 完成）
- **Flutter 3.47.2 stable 装到 `D:\flutter`**（Dart 3.13.2）。安装源走国内镜像绕开本机 GitHub 不稳 + Google 大文件被限速：
  - GitHub `git clone`/`git ls-remote` 均超时（本机到 Git 主机不稳）；`storage.flutter-io.cn` 直连 401；`mirrors.tuna.tsinghua.edu.cn` 404。
  - **可用源**：`storage.googleapis.com` 小文件快但大文件流 ~76KB/s 基本停滞；**`mirror.nju.edu.cn`（南大）7-13MB/s**，清单 URL `https://mirror.nju.edu.cn/flutter/flutter_infra_release/releases/releases_windows.json`，归档 `stable/windows/flutter_windows_<v>-stable.zip`。
  - 下载 1.93GB（152s），`unzip` 校验通过，解压到 `D:\flutter`。用户要求装 D 盘。
- 用户 PATH 已持久化加入 `D:\flutter\bin`（PowerShell `SetEnvironmentVariable('Path',...,'User')`）。本会话导出 `PUB_HOSTED_URL=https://pub.flutter-io.cn`、`FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn` 加速。
- **`flutter create .` 在 `src/app_flutter` 生成了 android/ios/web/windows 平台目录**（`analysis_options.yaml` 被更新 exclude 这些目录）。**未启 Windows 开发者模式**（插件符号链接需它，仅影响编译移动端插件，不影响 analyze/test）。
- **验证通过**：`dart analyze` 零问题；`flutter test` 9/9（自写 `test/format_test.dart` 纯函数单测，联网无关）。替换了 `flutter create` 默认 `widget_test.dart`（引用已不存在的 `MyApp`）。修复了 4 个 analyze 问题：collector 未用 `_client` 字段、models 悬空库注释、home_page `final`→`const`、test `Color.value`→`toARGB32`。

## Flutter 7 业务页补齐（2026-08-29 完成）
- 移殖 `src/app/src/pages/*` 的 uni-app 页面到 Flutter：`lib/features/app_shell.dart`（底部 5 Tab：实时/自选/复盘/龙虎榜/历史；appBar 图标进大盘/设置）+ `realtime`/`watchlist`/`overview`/`review`/`dragon`/`history`/`settings`/`kline`。
- 数据层扩了 `models.dart`（RealtimeSnapshot 加 breadth/sectors/limitUpStocks/pankou、DragonSeat/DragonItem）、`format.dart`（pctText/fmtNum/fmtWan/fmtTime，kUp/kDown/kFlat）、`providers.dart`（realtime/reviews/review/reviewByDate/dragon/status/watchlist/stocks）。
- 消费 `/api/*` 契约：realtime 指数条+宽度+涨停梯队+领涨板块+盘口异动；review 温度+宽窄+正文；dragon 席位展开；watchlist 增删+跳K线；overview 指数表+板块+两市成交额；history 列表→详情；settings 云端地址+状态。
- 删除旧 `home_page.dart`；`main.dart` 启动引导后进 `AppShell`。**`dart analyze` 零问题、`flutter test` 9/9 通过**。
- K线页为占位（数据经 `/api/kline` 下发，图表库后续引入）；采集仍由 collector_service 后台执行。

## 明暗两套主题皮肤（2026-08-29 完成，对齐 PC 版）
- 新增 `lib/core/theme.dart`：`AppPalette`（ThemeExtension，含 up/down/flat/gold/bg/card/ink/muted/line/brand）+ `buildLightTheme()`/`buildDarkTheme()`。色板取自 PC `index.html` :root/body.dark：light bg #f6f8f7/card #fff/红(涨) #e34845/绿(跌) #168b72；dark bg #10171d/card #1e2a33/红 #ff8b83/绿 #77d1b0。
- `format.dart` 删 `kUp/kDown` 硬编码 → `pctColor(context,pct)`（主题感知）；各页面（realtime/overview/watchlist/review/dragon）取红绿改走 `AppPalette.of(context)`；`main.dart` 接 `theme`+`darkTheme`+`themeMode`；`providers.dart` 加 `themeModeProvider`。
- 设置页新增「主题皮肤」SegmentedButton（跟随系统/亮/暗）。`dart analyze` 零问题、`flutter test` 7/7、CDP 运行 0 异常。

## K线图标（koboyo hand-drawn + 自绘）
- PC/移动端主题本机无 gpt-image-2；采用拿 koboyo 手绘 `candlestick` 真 SVG（`/icons/svg/candlestick.svg`）+ 代码重上色渲染 PNG。产出 `.runtime/icon_dark.png`（深色渐变底+品牌红发光 K线，推荐）与 `icon_white.png`。**未接入 App**（待定选型后走 flutter_launcher_icons / 桌面 icon.ico）。
- 教训：单路径蜡烛的空心/实体靠 SVG evenodd 挖孔，**拆分子路径染色会破坏挖孔** → 整图单色最稳；`/icons/<slug>` 详情页服务端渲染，内联 svg 常是 17px 白色点缀非主视觉，真资产在 `/icons/svg/<slug>.svg`。

## 决策：默认分享，无开关（2026-08-29 定稿，明天执行）
- **采集方 = PC + 移动端（众包），默认分享，不向用户暴露开关**。PC 常驻/可信/字段全，作高可信源；移动端补充，任一设备离线其余顶替，无单点。移动端不依赖 PC 在线。
- 明天只做两件事：① **部署 mapi 到 `api.dailystock.askcode.cn`**（47.92.170.168，自带 PG）；② **PC 默认分享 + 去开关**（storage `cloud_enabled:true`、`cloud_url` 内置默认=https://api.dailystock.askcode.cn、设置页移除"手机云同步"复选框与 token 输入、自动设备鉴权）。
- 方案落地文档：`docs/app-deploy-plan.md`（部署步骤/改代码点/验证/回滚/风险）。`docs/app-crowdsource-collection.md` 首段与 §二 已补"采集方构成 + 默认分享"决策。
- 待办：PG 连接信息、SSH 主机别名（候选 `~/.ssh/deploy_market_daily`，config 尚无）、MAPI_JWT_SECRET、.env（OSS+PG）、nginx 反代、systemd——明天执行。

